<%= form_with(model: agent_type, local: true) do |form| %>
  <% if agent_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(agent_type.errors.count, "error") %> prohibited this agent_type from being saved:</h2>

      <ul>
      <% agent_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.number_field :code, id: :agent_type_code %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :agent_type_description %>
  </div>

  <div class="field">
    <%= form.label :quant_hours %>
    <%= form.number_field :quant_hours, id: :agent_type_quant_hours %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
