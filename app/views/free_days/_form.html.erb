<%= form_with(model: free_day, local: true) do |form| %>
  <% if free_day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(free_day.errors.count, "error") %> prohibited this free_day from being saved:</h2>

      <ul>
      <% free_day.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :day %>
    <%= form.date_select :day, id: :free_day_day %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :free_day_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
