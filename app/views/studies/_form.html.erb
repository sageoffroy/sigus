<%= form_with(model: study, local: true) do |form| %>
  <% if study.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(study.errors.count, "error") %> prohibited this study from being saved:</h2>

      <ul>
      <% study.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :level %>
    <%= form.number_field :level, id: :study_level %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :study_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
