<h1>Cobertura y Cupo de MPG</h1>

<table class="datatable">
  <thead>
    <tr>
      <th>Agent</th>
      <th>Hs semana</th>
      <th>Gs semana</th>
      <th>Hs sabado</th>
      <th>Hs domingo</th>
      <th>Hs Guarias</th>
      <th>Hs Umu</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @monthly_for_guard_hours.each do |monthly_for_guard_hour| %>
      <tr>
        <td><%= monthly_for_guard_hour.agent %></td>
        <td><%= monthly_for_guard_hour.hs_semana %></td>
        <td><%= monthly_for_guard_hour.gs_semana %></td>
        <td><%= monthly_for_guard_hour.hs_sabado %></td>
        <td><%= monthly_for_guard_hour.hs_domingo %></td>
        <td><%= monthly_for_guard_hour.hs_total_1 %></td>
        <td><%= monthly_for_guard_hour.hs_total_2 %></td>
        <td>
          <%= link_to (fa_icon "eye"), monthly_for_guard_hour%>
          <%= link_to (fa_icon "pencil-square-o"), edit_monthly_for_guard_hour_path(monthly_for_guard_hour)%>
          <%= link_to (fa_icon "trash-o"), monthly_for_guard_hour, method: :delete, data: { confirm: '¿Seguro desea eliminar este registro?'} %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'ir a Controles de Cupo', '/mpg_controls', class: 'btn btn-primary btn-xs' %>
<%= link_to 'Crear nuevo Covertura/Cupo', new_monthly_for_guard_hour_path, class: 'btn btn-primary btn-xs' %>
