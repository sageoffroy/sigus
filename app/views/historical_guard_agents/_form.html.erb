<%= form_with(model: historical_guard_agent, local: true) do |form| %>
  <% if historical_guard_agent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(historical_guard_agent.errors.count, "error") %> prohibited this historical_guard_agent from being saved:</h2>

      <ul>
      <% historical_guard_agent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :agent_id %>
    <%= form.text_field :agent_id, id: :historical_guard_agent_agent_id %>
  </div>

  <div class="field">
    <%= form.label :month %>
    <%= form.number_field :month, id: :historical_guard_agent_month %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year, id: :historical_guard_agent_year %>
  </div>

  <div class="field">
    <%= form.label :concept %>
    <%= form.text_field :concept, id: :historical_guard_agent_concept %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :historical_guard_agent_description %>
  </div>

  <div class="field">
    <%= form.label :hs_total %>
    <%= form.number_field :hs_total, id: :historical_guard_agent_hs_total %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
